# handoff — smriti

## pick up

### 1. implement find --path/--ext (wave 1 from plan)

Add path glob and extension search to `smriti find`. Currently `find` only does FTS on document content — searching for `.iso` returns nothing. Need a SQL query against the `paths` table with LIKE or glob matching.

Plan: `docs/plans/triage-find-audit-improvements.md`

### 2. implement audit compact mode (wave 2 from plan)

Default `smriti audit` to a compact summary (top 5 extensions, top 5 tier-2). Add `--full` for current verbose output and `--ext .iso` to drill into specific extensions.

### 3. triage canonical scoring + directory grouping (waves 3-4)

Triage picks the wrong canonical for duplicates when one copy is in a cataloged dir like ~/Downloads. Need to load smritiignore rules into triage and score paths. Then collapse directory-level duplicates.

## context

- Current version: 0.2.3 (installed)
- Josh deleted ~/.smritiignore and recreated it fresh after the glob escaping fix
- Josh has not yet added `export EDITOR=nvim` to shell profile
- The plan doc has full details on all four improvements
