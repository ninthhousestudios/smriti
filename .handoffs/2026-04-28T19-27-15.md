# handoff

## just completed

Fixed MCP server compatibility across Gemini CLI and OpenCode:
- smriti: `smriti_health` schema fixed (null → object), binary rebuilt, service restarted
- sangha/sutra: tracing redirected to stderr in stdio mode, binaries rebuilt and installed
- qartez removed from all client configs (Claude Code, Gemini CLI, OpenCode), replaced with sutra
- sutra-guard hooks installed in Gemini CLI config
- OpenCode AGENTS.md updated from qartez to sutra instructions

## to pick up

- **OpenCode retest**: sangha -32000 error should be fixed by the stderr fix but wasn't retested in OpenCode. Try launching OpenCode and checking `/mcp` or equivalent.
- **smriti scan**: PID 4312 was finalizing a 950K-file scan over USB. Check if it completed. The O(n²) inode lookup fix is committed but only helps future scans.
- **Triage plan**: all 4 waves from `docs/plans/triage-find-audit-improvements.md` were completed before this session. No remaining work there.
## pick up next
- **MCP parity**: `FindParams` in `src/mcp.rs` doesn't expose `--path` / `--ext` yet. Add optional `path` and `ext` fields and wire them to `search_path` / `search_extension`.
- **audit --tier2 perf**: currently runs the full audit query even when only tier2 data is needed. Could add a separate tier2-only query if it becomes a bottleneck.
- **search_extension perf**: uses `LOWER(p.path) LIKE` which scans the full paths table. If slow on large DBs, consider a generated column or index on the extension.
- **triage testing**: no unit tests for `canonical_score` or dir-pair collapsing. Worth adding if the scoring logic gets more complex.

## context

- `SectionRules` (ignore crate's `Gitignore`) does not implement `Clone`. We added `SectionRules::classify()` as the public API instead of requiring ownership.
- Dir-pair collapsing keys on `(canonical_parent, dup_parent)` using wave-3 scoring order, not lexical. Inconsistent scoring across a pair means files stay individual (correct behavior).

## context

- smriti-serve runs as systemd user service (`systemctl --user restart smriti-serve`)
- sangha/sutra are stdio-spawned by each client, no persistent daemon
- Gemini CLI config: `~/.gemini/settings.json`
- OpenCode config: `~/.config/opencode/opencode.json`
- Claude Code config: `~/.claude/settings.json`
