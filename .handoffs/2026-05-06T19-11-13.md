# Handoff

Last updated: 2026-05-06

## What to pick up next

**smriti/22** — Finish watcher integration tests. The FK fix (creating a scan_runs row at watcher startup) is coded but the tests haven't been re-run. This is the only blocker before marking smriti/11 done.

Steps:
1. Run `cargo test --test watcher_test -- --test-threads=1` to verify the FK fix works
2. Replace `eprintln!("[watcher] ...")` in `src/watcher.rs` with `tracing::debug!`/`tracing::info!`
3. Remove unused `use std::sync::Arc` import in watcher.rs
4. Run full test suite, commit, mark smriti/11 and smriti/22 done

## Watcher pipeline status

Tasks 7–10 are done. Task 11 (minimal watcher) is WIP at commit `7d5a057`:
- `src/watcher.rs` — full pipeline: notify → debounce → process_path → DB
- `src/debounce.rs` — pure data structure, 11 tests passing
- `migrations/0003_watcher_tables.sql` — scan_requests + watcher_heartbeat
- `src/db.rs` — acquire_writer_lock with flock(2)
- `tests/watcher_test.rs` — 2 integration tests (create/modify/delete lifecycle + new directory children)

## After smriti/11

Check `yojana_query project=smriti` for the full dependency chain. Next tasks that become unblocked:
- smriti/12: Watcher move detection (MOVED_FROM/TO end-to-end)
- smriti/13: Startup full scan + periodic safety-net scan driver
- smriti/18: Graceful shutdown with debounce drain deadline

## Notable context

- `notify` v7 is used (v8 available but untested)
- The watcher uses a split API: `run_watch()` for CLI (installs signal handlers), `run_watch_with_shutdown(&AtomicBool)` for tests
- FK constraint: process_path requires a valid scan_id referencing scan_runs — watcher creates a synthetic row at startup
