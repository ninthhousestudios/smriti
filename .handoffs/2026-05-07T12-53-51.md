# Handoff

## Current state

smriti v0.3.0 released and installed. smriti-watch.service is enabled and running as a systemd user service.

## What to pick up next

### Verify smriti-watch service stability
- `systemctl --user status smriti-watch` — confirm it's active
- `kill -9 $(pidof smriti)` then check it restarts (RestartSec=2)
- `systemctl --user stop smriti-watch` — confirm clean shutdown (heartbeat → stopped)

### Design smriti→sutra notification wire (smriti/23)
Key questions to resolve:
- Transport: sutra is session-scoped (starts/stops with MCP sessions), smriti is always-on. Polling smriti's events table (with `id > last_seen` or `timestamp > X`) is simplest and matches the lifecycle mismatch.
- Whether sutra should persist its index between sessions (prerequisite for incremental updates to be useful)
- Catch-up: sutra could query events since its last index timestamp on startup

## Context

- The watcher's event stream is in the `events` table (content_hash, path, event_type, timestamp)
- `walk_roots` applies `hardened_defaults(root)` internally regardless of what global_rules are passed — both scanner and watcher startup scan use this path
- The parity test (`cargo test --test scan_parity_test -- --ignored`) validates scanner/watcher equivalence

---

## 2026-05-07 — qi comparison session (append)

Reviewed qi (`/home/josh/soft/qi`) and confirmed smriti's perception-tier identity. Two new needs-triage tasks:

- **smriti/28** — make graceful-tiering explicit in docs AND retire the dense-embedding feature flag. Decision in chitta: `smriti-no-embedding-2026-05-07`. Reasoning: embedding the same shallow material (title + topics + 100KB excerpt) doesn't extend smriti's reach; expanding to full content storage would blur the smriti/kosha seam. Anything that needs semantic depth lives in kosha. Cleanup: remove `embedding` feature flag, BGE-M3 ONNX path, unwired `search_hybrid`, `SMRITI_MODEL_PATH` docs.
- **smriti/29** — auto-derive friendly aliases for roots (e.g. `~/projects/tools/qi` → `tools-qi`). UX win for both humans and agents using `find -r`. Reference: qi's collection-name derivation in `/home/josh/soft/qi/internal/config/`.

Reference repo stays at `/home/josh/soft/qi`.
