# Handoff

Last updated: 2026-05-06

## What to pick up next

The watcher daemon is fully planned and decomposed. Three slices are ready to start (no blockers, can run in parallel):

1. **smriti/7 — Extract per-path core from scanner.rs** (critical path)
   The load-bearing prerequisite. Extract `process_path()` from the 1075-line `scanner.rs`. Everything downstream depends on this.

2. **smriti/8 — Scan setup consolidation** (bug fix)
   MCP scans ignore user smritiignore rules. Extract `prepare_scan()` into library code.

3. **smriti/9 — Debounce buffer with move cookie pairing** (pure logic)
   Standalone in-memory data structure. No I/O. Can be built and tested independently.

After those three land, **smriti/10** (schema + writer lock) unblocks, then **smriti/11** (minimal watcher tracer bullet) — the first end-to-end slice.

## Where things live

- **PRD**: yojana smriti/6 (also has the full description inline)
- **All 15 slices**: yojana smriti/7-21 with dependency edges
- **Design spec**: `docs/daemon-design-spec.md` (canonical technical design)
- **ADRs**: `docs/adr/0001-0004` (single-writer, DB-only coordination, audit.db, one-path-per-tx)
- **Refactor candidates**: `docs/smriti-overall-refactor.md` (candidates #1 and #2 are watcher prereqs, mapped to smriti/7 and smriti/8)

## Context for next session

- smriti is at v0.2.4. Two commits this session: find --path ESCAPE fix + --limit flag, and --version flag.
- Yojana quirk: `ready-for-agent` status can't be set at task create time — defaults to `needs-triage`, requires a follow-up update call.
- smriti/1-3 (events API, blobs, revert) are independent tracks, still `needs-triage`. Not related to the watcher work.
- smriti/4-5 are `wontfix` (superseded by the new slices).
- `todo.md` and `docs/todo.md` in repo root are stale personal notes from earlier sessions.
